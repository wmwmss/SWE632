import React from "react";

const Notes = () => {
    return (
        <div>
            Notes:
                <p>1. To select multiple items in order: Click on the first item, press <em>Shift</em> key, then click on the last item, and release <em>Shift</em> key.</p>
                <p>2. To select multiple non-sequential items: Click on the first item, press and hold <em>Ctrl</em> key, click to select other items, and release <em>Ctrl</em> key after selection finished. </p>
        </div>
    )
}

export default Notes;